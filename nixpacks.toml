[phases.setup]
nixPkgs = ["python312", "chromium", "nss", "nspr", "atk", "at-spi2-atk", "libdrm", "xorg.libxcb", "libxkbcommon", "noto-fonts-cjk"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "python -m playwright install chromium"
]

[start]
cmd = "python bot.py"
